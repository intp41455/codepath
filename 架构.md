# 架构说明

本页解释「循码 Codepath」的模块划分、数据流与安全模型。适合想在仓库里改代码 / 加课程 / 修 bug 的人。

## 1. 总体形态

- **纯静态**：没有后端、没有框架、没有构建步骤。`dist/` 就是整站，clone 后本地起 HTTP 服务即可运行。
- **单一 HTML 入口**：`dist/index.html` 引入十余个 `<script defer>`，各模块以全局变量 / 函数协作，按 `location.hash` 做前端路由。
- **业务与界面分离**：可被测试复用的纯逻辑集中在 `dist/learning-core.js`，页面（`app.js`）与回归测试（`tests/regression.cjs`）都调用它。

## 2. 目录与模块职责

```
dist/
├─ index.html          # 唯一入口：导航 / 顶栏 / 主区 / 页脚 / 帮助 dialog
├─ style.css           # 桌面 + 手机（375px）布局，深浅主题变量
├─ app.js              # 路由、编辑区、运行编排、备份导入导出、modelContext 工具注册
├─ learning-core.js    # ★ 纯业务：XP、每日奖励去重、状态归一化、备份合并、静态风险扫描
├─ curriculum.js       # 数据骨架：window.TRACKS / LESSONS / PROJECTS 的初始化
├─ foundations.js      # Python 基础 + 数据结构课程
├─ backend.js          # SQL / 开发工具 / FastAPI / Java / Spring 课程
├─ ai-lessons.js       # Agent / 检索增强 / 多 Agent / 智能体系统课程
├─ mastery.js          # 源码阅读 / AI 代码理解 / 复杂项目工程课程
├─ projects.js         # 综合项目数据（10 个）+ FASTAPI 参考实现
├─ new-courses.js      # v2 新增课程线（JS / TS / Linux / 智能体）
├─ new-projects.js     # v2 新增项目（前端 / Linux / 协作协议 / 网站测试，共 4 个 → 14）
├─ daily.js            # 14 个每日挑战（算法 + AI 代码阅读）
├─ learning-hub.js     # 首页、每日挑战、审核与测试中心、掌握自评面板
├─ sandbox.js          # ★ 隔离执行：不透明源 iframe + 一次性 Worker
├─ runner.js           # Pyodide 版 Python / SQLite 运行器（Worker 内）
├─ js-runner.js        # JS / TS 运行器：strict 类型检查 → 编译 → 执行 + 断言
├─ capstones.js        # 毕业证据自评（证据式打分 + 导出）
├─ export-dialog.js    # 导出下载 + 复制回退 dialog
├─ test-report.html    # 实测报告（浏览器 E2E 记录）
├─ codepath-tests.zip  # 可下载测试包（scripts/build-test-bundle.py 生成，34 成员）
└─ vendor/typescript/  # 随仓库分发的 TS 5.9.3 编译器（含 LICENSE）
```

## 3. 数据模型（课程 / 项目 / 挑战）

课程小节（lesson）的关键字段：

| 字段 | 含义 |
| --- | --- |
| `id` / `track` | 唯一标识 / 所属路径 |
| `kind` | `python` / `sql` / `javascript` / `typescript` / `guided` |
| `title` / `intro` / `concept` / `example` / `explain` | 讲解四段 |
| `steps` / `reflection` | 动手步骤 + 迁移反思 |
| `starter` / `solution` / `hint` | 初始代码 / 参考答案 / 提示 |
| `test` / `expected` | 可执行小节的判定（断言或结果比对） |
| `checks` / `quiz` | 本机指导（`guided`）小节的自查清单 + 知识题 |
| `source` / `minutes` / `filename` | 官方资料链接 / 预计时长 / 文件名 |

项目（project）：`stages`（多步骤 + 参考代码）+ `checks`（验收清单）+ `challenge`（独立挑战）+ 可选 `capstone`（毕业）。
当前规模：**129 小节 · 17 路径 · 14 项目 · 14 每日挑战**，全部在 `tests/regression.cjs` 有数量与字段断言。

## 4. 运行 / 安全模型（最重要）

学习者代码**不能**访问站点本身，靠三层构造性保证：

1. **不透明源 iframe**（`sandbox.js`）
   - `<iframe sandbox="allow-scripts">` 且**不含** `allow-same-origin` → 帧与站点不同源，读不到站点 `localStorage` / DOM。
   - CSP 限制：`default-src 'none'`；`connect-src` 仅允许同源的 Pyodide 自托管目录 `pyodide/`（`js`/`ts` 允许 blob）。

2. **一次性 Worker + 双超时**
   - 每段代码用新 Blob 起一个 Worker，跑完即 `terminate()`，无残留状态。
   - 执行 5 秒超时（杀掉无限循环）、加载 90 秒超时（首次下载环境）。

3. **真实但受限的解释器**
   - Python / SQL：Pyodide + SQLite，SQL 每次重建 `:memory:` 数据库；输出截断 20000 字符、结果截断 2000 行。
   - JS / TS：内联 TS 5.9.3 编译器做 strict 检查 → 编译 → 执行；断言计数区分「通过 / 没断言 / 业务错」。
   - **判定底线**：无断言不算通过；注释 / 死代码断言不算数；业务错但语法对也会被拒。

> 注意：这些是「构造上阻止」，不等于「完整漏洞审计 / 生产安全保证」。静态风险扫描（`learning-core.js` 的 `review`）是启发式，有误报漏报。

## 5. 状态与持久化

- 全局 `state`（`app.js` 顶部）= `{version:1, current, done, drafts, notes, checks, projectChecks, viewedSolutions, projectEvidence, dailyRewards, dailyDrafts, mastery}`。
- 所有读写先经 `LearningCore.normalize(...)`（`learning-core.js`）做**白名单归一化**：未知键丢弃、类型不符丢弃、越界截断、日期校验（`validDay`）。因此**畸形备份 / 旧版本备份都安全迁移**，不会变成可被注入的 UI 数据。
- 备份合并用 `LearningCore.merge(a, b, ...)`，按键合并后重新 `normalize`；每日奖励用 `LearningCore.reward` 保证「同一天只记一次」，XP 用 `LearningCore.xp` 按唯一完成记录计算，**不重复累加**。
- 存储键：`codepath-learning-v1`（localStorage）。

## 6. 测试体系（为什么可信）

`tests/regression.cjs` 用 `node:vm` 在受控 context 里加载真实课程 + 真实运行器，覆盖：

- 课程 ID / 先修 / 练习契约完整（数量 129 / 14）；
- 所有交付 JS 可解析；
- 每个 JS/TS 小节的「参考解通过 / 未完成 starter 被拒」；
- TS 拒绝语义错误（不是仅语法错误）；
- 无断言 / 死断言不通过；输出洪水报错；
- 每日奖励跨提交 / 备份合并幂等；
- 静态扫描命中种子风险 + 保留安全负向对照。

Python 侧 `exercises.py`（51 参考 + 10 反例）、`runtime-python.py`（51 + 7）执行作者编写的可信参考实现——**它不是沙箱，不要用它跑陌生代码**。

## 7. 数据流示意

```
用户输入(代码)
   │
   ▼
app.js runLesson()  ──postMessage──▶  sandbox.js（不透明 iframe）
   │                                      │
   │                                      ▼  按 kind 选择
   │                    runner.js(Py/SQL) 或 js-runner.js(JS/TS)
   │                              （Worker + TS 编译 + 断言）
   ▼
结果 {output, passed, checks, feedback, error}
   │
   ▼
learning-hub / app 渲染结果；passed → markDone → save() → localStorage
   │
   ▼
LearningCore.* 决定 XP / 去重 / 归一（与测试共享同一份逻辑）
```
