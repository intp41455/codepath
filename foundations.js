const add=l=>LESSONS.push({minutes:18,kind:'python',...l});
TRACKS.push(
{id:'structures',name:'数据结构与算法',symbol:'[]',color:'#b27a2e',tint:'#fff6e5',desc:'理解列表、字典、栈、队列和查找，让程序处理更多数据。',prereq:'Python 基础',outcome:'解释数据组织方式，并选择合适的算法',source:'https://docs.python.org/zh-cn/3/tutorial/datastructures.html'},
{id:'sql',name:'SQL 与数据库',symbol:'DB',color:'#9d63ad',tint:'#f6edfa',desc:'查询、关联和汇总真实练习数据，学会持久保存信息。',prereq:'变量与条件判断',outcome:'设计记账表并写出统计查询',source:'https://www.sqlite.org/lang.html'},
{id:'engineering',name:'开发工具与工程基础',symbol:'>_',color:'#61848d',tint:'#eaf5f6',desc:'从文件和终端开始，掌握 Git、HTTP、测试和问题定位。',prereq:'Python 基础',outcome:'在自己的电脑上运行、测试并管理项目',source:'https://git-scm.com/book/zh/v2'},
{id:'fastapi',name:'FastAPI 接口开发',symbol:'⚡',color:'#238578',tint:'#e8f6f0',desc:'把 Python 函数变成别人可以访问的接口。',prereq:'Python、SQL、HTTP 与开发工具',outcome:'完成可验证的任务管理 API',source:'https://fastapi.tiangolo.com/tutorial/'},
{id:'java',name:'Java 与面向对象',symbol:'Jv',color:'#b8664a',tint:'#fcf0e9',desc:'理解类型、类、接口、集合和异常，建立第二种语言思维。',prereq:'Python、数据结构与开发工具',outcome:'编写并测试一个 Java 任务管理程序',source:'https://dev.java/learn/'},
{id:'springboot',name:'Spring Boot 应用开发',symbol:'Sb',color:'#67983e',tint:'#f0f6e7',desc:'学习依赖注入、分层设计、校验、数据库与接口测试。',prereq:'Java、SQL、HTTP',outcome:'构建具有清晰分层的 Java 后端',source:'https://spring.io/guides/gs/spring-boot'},
{id:'springai',name:'Spring AI 应用集成',symbol:'Ai',color:'#477e94',tint:'#eaf3f8',desc:'连接模型、约束输出、调用工具，并用评估检验结果。',prereq:'Spring Boot 与测试',outcome:'把可追踪的 AI 功能接入 Java 服务',source:'https://docs.spring.io/spring-ai/reference/'},
{id:'agent',name:'Agent 与检索增强',symbol:'A↗',color:'#8060bb',tint:'#f0ecfb',desc:'拆开 Agent 的状态、模型、工具、记忆和评估。',prereq:'Python、接口与测试',outcome:'实现有工具边界与停止条件的助手',source:'https://www.anthropic.com/engineering/building-effective-agents'},
{id:'multi',name:'多 Agent 协作',symbol:'A²',color:'#ac6284',tint:'#f9eef3',desc:'分工、传递状态、并行汇总、失败恢复，控制协作成本。',prereq:'Agent 基础、数据结构与测试',outcome:'实现可检查、可恢复的多角色工作流',source:'https://www.anthropic.com/engineering/building-effective-agents'},
{id:'source',name:'GitHub 源码阅读与分析',symbol:'</>',color:'#54719a',tint:'#ecf1f8',desc:'面对陌生仓库，有方法地定位入口、追踪调用和验证理解。',prereq:'至少完成一条后端路线与综合项目',outcome:'独立交付项目结构图、调用链、分析报告和一次修改',source:'https://docs.github.com/en/get-started/exploring-projects-on-github'}
);
add({id:'py-02',track:'python',title:'变量：给数据起个名字',intro:'如果一句问候里的人名每天都不同，我们不必改整段程序。把名字保存在变量里，再重复使用它。',concept:'赋值符号 = 把右侧的值交给左侧名称。字符串是文字，整数是没有小数的数。变量名用英文、数字和下划线，不能以数字开头。',example:'name = "小林"\nprint(name)',explain:'name 是标签，"小林" 是值。print(name) 取出变量的值；print("name") 只会显示四个字母。',steps:['创建变量 name，值为 "小白"。','创建变量 days，值为整数 1。','用两行 print 分别显示它们。'],starter:'# 定义 name 和 days，然后输出\n',solution:'name = "小白"\ndays = 1\nprint(name)\nprint(days)',test:'assert name == "小白", "name 应为字符串 小白"\nassert type(days) is int and days == 1, "days 应为整数 1"\nassert _output == "小白\\n1", "分两行输出 name 和 days"',hint:'文字需要引号，数字 1 不需要引号。',reflection:'把 days 改为 "1"，它还是整数吗？'});
add({id:'py-03',track:'python',title:'计算与类型转换',intro:'做一个购物小计算器：每本书 25 元，买 3 本，应付多少？',concept:'* 做乘法。来自表单或文件的 "3" 是文字，int("3") 才是整数。不同类型不能随意相加。',example:'price = 12\ncount = int("2")\nprint(price * count)',explain:'int 先将文字转换为数，再参与计算。把 "两本" 传给 int 会报错，因为它不是数字文本。',steps:['把 count_text 转换为整数 count。','计算 total，为 price 与 count 的乘积。','显示 total，应为 75。'],starter:'price = 25\ncount_text = "3"\n# 转换、计算、输出\n',solution:'price = 25\ncount_text = "3"\ncount = int(count_text)\ntotal = price * count\nprint(total)',test:'assert count == 3 and type(count) is int\nassert total == 75\nassert _output == "75"',hint:'count = int(count_text)，然后用 * 相乘。',reflection:'25 * "3" 会得到什么？为什么不是 75？'});
add({id:'py-04',track:'python',title:'字符串：生成一条消息',intro:'程序经常需要把文字和数字拼成一句话，比如提醒你已经学了几天。',concept:'f 字符串在引号前加 f，在花括号里写变量。它会把变量的值嵌入文字中。',example:'name = "小林"\nprint(f"你好，{name}")',explain:'花括号里面是变量名，不要再加引号。字符串也支持 strip() 去掉两边空白，len() 计算长度。',steps:['用 strip() 清理 name 两侧空格。','用 f 字符串生成 message：小白已学习3天。','输出 message。'],starter:'name = " 小白 "\ndays = 3\n# 清理 name，再生成 message\n',solution:'name = " 小白 "\ndays = 3\nname = name.strip()\nmessage = f"{name}已学习{days}天"\nprint(message)',test:'assert name == "小白"\nassert message == "小白已学习3天"\nassert _output == message',hint:'先 name = name.strip()，再把 name 和 days 放进花括号。',reflection:'为什么 f"{days}" 和 "{days}" 的结果不同？'});
add({id:'py-05',track:'python',title:'条件判断：让程序做选择',intro:'不同分数得到不同反馈。程序需要比较条件，再决定执行哪一段。',concept:'if 表示“如果”；else 表示“否则”。条件后有冒号，属于这一分支的代码缩进四个空格。>= 表示大于或等于。',example:'score = 80\nif score >= 60:\n    print("通过")\nelse:\n    print("再练一次")',explain:'= 是赋值，== 是比较是否相等。Python 用缩进确定代码的归属，不是靠装饰排版。',steps:['当 age 大于或等于 18，把 status 设为 "成年"。','否则设为 "未成年"。','输出 status；当前 age 为 17。'],starter:'age = 17\n# 写出 if / else\n',solution:'age = 17\nif age >= 18:\n    status = "成年"\nelse:\n    status = "未成年"\nprint(status)',test:'assert status == "未成年"\nassert _output == "未成年"',hint:'if age >= 18: 后面的赋值语句需要缩进。',reflection:'把 age 改为 18 手动测试。为什么边界值也要测？'});
add({id:'py-06',track:'python',title:'列表：保存一组数据',intro:'一个变量只能指向一个值，但这个值可以是一整个列表，用来保存多项任务。',concept:'列表使用方括号。索引从 0 开始，tasks[0] 是第一项。append 添加到末尾，len 返回元素数量。',example:'tasks = ["阅读", "练习"]\ntasks.append("复盘")\nprint(tasks[0])',explain:'第三项的索引是 2，不是 3。访问不存在的索引会引发 IndexError。',steps:['创建 tasks，依次包含 "学习"、"练习"。','用 append 添加 "复盘"。','将第一项保存到 first，并输出列表长度。'],starter:'# 创建 tasks 并添加新任务\n',solution:'tasks = ["学习", "练习"]\ntasks.append("复盘")\nfirst = tasks[0]\nprint(len(tasks))',test:'assert tasks == ["学习","练习","复盘"]\nassert first == "学习"\nassert _output == "3"',hint:'列表用 [ ]，取第一项用 tasks[0]。',reflection:'空列表能取第 0 项吗？如何先判断它是否为空？'});
add({id:'py-07',track:'python',title:'循环：把重复的事交给电脑',intro:'想计算几笔花费之和，可以手动相加，也可以让电脑逐项处理。',concept:'for 把列表里的每一项依次交给变量。total += amount 等价于 total = total + amount。累计前先把 total 设为 0。',example:'total = 0\nfor amount in [2, 4, 6]:\n    total += amount\nprint(total)',explain:'total 应在循环外初始化。如果放在循环内，每处理一项都会被清零。',steps:['创建 total，初始值为 0。','遍历 expenses，将每项加入 total。','输出总额，应为 60。'],starter:'expenses = [10, 20, 30]\n# 用 for 循环累计\n',solution:'expenses = [10, 20, 30]\ntotal = 0\nfor amount in expenses:\n    total += amount\nprint(total)',test:'assert total == 60\nassert _output == "60"',hint:'循环的三步：初始化累计值、逐项遍历、更新累计值。',reflection:'如果 expenses 为空，你的程序会输出什么？'});
add({id:'py-08',track:'python',title:'字典：用名称查数据',intro:'一名学生有姓名和分数。用位置记住每个字段容易混淆，字典可以用名称查找。',concept:'字典以 {键: 值} 保存数据。student["name"] 根据键取值；get("city", "未知") 在缺失时返回默认值。',example:'student = {"name": "小林", "score": 80}\nprint(student["name"])',explain:'字典键通常是字符串。直接访问不存在的键会报 KeyError，而 get 可以处理预期的缺失。',steps:['创建 student，name 为 "小白"，score 为 90。','把 score 更新为 95。','用 get 读取不存在的 city，默认 "未知"，保存到 city。'],starter:'# 创建并更新 student\n',solution:'student = {"name": "小白", "score": 90}\nstudent["score"] = 95\ncity = student.get("city", "未知")\nprint(student)',test:'assert student == {"name":"小白","score":95}\nassert city == "未知"',hint:'更新字段：student["score"] = 95。',reflection:'列表适合保存多个学生；字典适合保存一个学生的字段。两者如何组合？'});
add({id:'py-09',track:'python',title:'函数：封装可以重复使用的动作',intro:'把“计算两数之和”起个名字，就能在不同地方反复调用，不再重复写相同代码。',concept:'def 定义函数，参数是输入，return 是交回调用者的结果。print 只显示内容，不能替代 return。',example:'def double(number):\n    return number * 2\n\nprint(double(4))',explain:'定义函数时不会立刻执行里面的代码。double(4) 才是调用；4 会成为参数 number 的值。',steps:['定义函数 add(a, b)。','返回 a 与 b 的和。','输出 add(3, 4)，检查会另外测试负数和 0。'],starter:'def add(a, b):\n    # 把 pass 换成返回语句\n    pass\n\nprint(add(3, 4))',solution:'def add(a, b):\n    return a + b\n\nprint(add(3, 4))',test:'assert add(3,4)==7, "3 + 4 应为 7"\nassert add(-2,5)==3, "需要支持负数"\nassert add(0,0)==0',hint:'函数体需要 return a + b。',reflection:'如果只在函数中 print(a + b)，调用者拿到的值是什么？'});
add({id:'py-10',track:'python',title:'组合判断与循环',intro:'你已经会判断、循环和函数。现在把它们组合起来，筛选及格分数。',concept:'在 for 内放 if，就能逐项判断。将符合条件的值 append 到新列表，再 return 新列表。',example:'def positives(numbers):\n    result = []\n    for n in numbers:\n        if n > 0:\n            result.append(n)\n    return result',explain:'return 必须在循环完成后执行。否则函数处理完第一项就会提前结束。',steps:['完成 passing(scores) 函数，保留所有大于或等于 60 的分数。','保持原有顺序，不修改输入列表。','空列表应该返回空列表。'],starter:'def passing(scores):\n    result = []\n    # 筛选及格分数\n    return result\n\nprint(passing([55, 60, 80]))',solution:'def passing(scores):\n    result = []\n    for score in scores:\n        if score >= 60:\n            result.append(score)\n    return result\n\nprint(passing([55, 60, 80]))',test:'assert passing([55,60,80]) == [60,80]\nassert passing([]) == []\nassert passing([59,60,61]) == [60,61]\nx=[80,20]; passing(x); assert x==[80,20], "不要修改输入列表"',hint:'在循环中 if score >= 60:，再 result.append(score)。',reflection:'为什么专门检查 59、60、61？这叫边界测试。'});
add({id:'py-11',track:'python',title:'异常：理解报错，而不是害怕它',intro:'用户可能把“十岁”输入年龄框。程序可以说明问题，而不是直接停止。',concept:'try 尝试执行，except ValueError 只处理无法转换数字的错误。不要把所有异常一律吞掉，否则真实问题会被隐藏。',example:'try:\n    count = int("三")\nexcept ValueError:\n    print("请输入数字")',explain:'阅读报错时先看最后一行的错误类型，再看它指向你的哪一行。NameError 通常是名称不存在；SyntaxError 通常是语法不完整。',steps:['完成 safe_int(text)，能转换则返回整数。','当 int 发生 ValueError 时，返回 None。','测试 "12"、"abc" 和负数。'],starter:'def safe_int(text):\n    # 使用 try 和 except\n    pass\n\nprint(safe_int("abc"))',solution:'def safe_int(text):\n    try:\n        return int(text)\n    except ValueError:\n        return None\n\nprint(safe_int("abc"))',test:'assert safe_int("12")==12\nassert safe_int("abc") is None\nassert safe_int("-3")==-3',hint:'except ValueError: 分支里 return None。',reflection:'为什么不推荐 except: 后面什么也不做？'});
add({id:'py-12',track:'python',title:'JSON：让数据可以保存和交换',intro:'函数里的字典只存在于内存中。为了写入文件或发给接口，需要转换成文本格式。',concept:'JSON 是常用的数据交换格式。json.dumps 把对象转成 JSON 字符串；json.loads 把字符串读回对象。',example:'import json\ntext = json.dumps({"done": False})\nprint(json.loads(text)["done"])',explain:'JSON 中布尔值写作 true/false，Python 中写作 True/False。用标准库转换，不要靠手工替换字符串。浏览器练习环境里的文件不是你的电脑文件。',steps:['导入 json。','将 task 转为 JSON 字符串 text。','把 text 读回字典 restored，保持中文可读可以使用 ensure_ascii=False。'],starter:'import json\ntask = {"title": "学习", "done": False}\n# 编码，再解码\n',solution:'import json\ntask = {"title": "学习", "done": False}\ntext = json.dumps(task, ensure_ascii=False)\nrestored = json.loads(text)\nprint(text)',test:'assert isinstance(text,str)\nassert json.loads(text)==task\nassert restored==task',hint:'text = json.dumps(task, ensure_ascii=False)，再 json.loads(text)。',reflection:'JSON 可以直接保存一个函数吗？数据和行为有什么区别？'});
add({id:'py-13',track:'python',title:'类与对象：把状态和行为放在一起',intro:'一个计数器既需要记住当前数值，也需要提供增加操作。类可以把两者组织在一起。',concept:'class 定义一种对象。__init__ 初始化状态，self 指向当前对象。不同实例拥有各自的状态。',example:'class Box:\n    def __init__(self):\n        self.items = []\n\nb = Box()\nprint(b.items)',explain:'实例属性写为 self.value。放在 __init__ 内初始化，能避免不同实例意外共享可变数据。',steps:['定义 Counter 类，初始 self.value 为 0。','定义 inc(self)，使 value 加 1，并返回新值。','创建两个实例，确认它们独立计数。'],starter:'class Counter:\n    def __init__(self):\n        self.value = 0\n\n    def inc(self):\n        pass\n\nc = Counter()\nprint(c.inc())',solution:'class Counter:\n    def __init__(self):\n        self.value = 0\n\n    def inc(self):\n        self.value += 1\n        return self.value\n\nc = Counter()\nprint(c.inc())',test:'a=Counter(); b=Counter()\nassert a.inc()==1\nassert a.inc()==2\nassert b.value==0',hint:'inc 里先 self.value += 1，再 return self.value。',reflection:'如果把 value 写成普通局部变量，会在下次调用时保留吗？'});
add({id:'py-14',track:'python',title:'写下测试：用证据确认程序正确',intro:'一次运行得到正确答案，不能说明所有输入都正确。测试会替你反复检查那些重要情况。',concept:'assert 条件 在条件为假时抛出 AssertionError。先写清楚输入和预期，再实现函数，这样你知道要解决什么。',example:'def square(n):\n    return n * n\n\nassert square(3) == 9\nassert square(0) == 0',explain:'测试通常包括正常值、边界值、空输入和错误输入。这里修复一个只能处理正数的错误实现。',steps:['修复 absolute(n)，返回 n 的绝对值。','至少自己加入 -5、0、3 三条 assert。','运行检查，确认没有 AssertionError。'],starter:'def absolute(n):\n    return n\n\n# 添加测试，然后修复函数\n',solution:'def absolute(n):\n    if n < 0:\n        return -n\n    return n\n\nassert absolute(-5) == 5\nassert absolute(0) == 0\nassert absolute(3) == 3\nprint("测试通过")',test:'assert absolute(-5)==5\nassert absolute(0)==0\nassert absolute(3)==3\nassert absolute(-0.5)==0.5',hint:'负数需要取反：return -n；其余直接 return n。',reflection:'测试能证明没有任何 bug 吗？它能可靠地证明哪些事情？'});
add({id:'ds-01',track:'structures',title:'复杂度：数据变多时会怎样',intro:'算法不只看一组数据跑得快不快，还要看输入增长时，工作量如何增长。',concept:'线性查找按顺序比较，最坏需要 n 次，记作 O(n)。列表按索引读取通常为 O(1)。大 O 描述增长趋势，不是精确秒数。',example:'for index, value in enumerate([7, 3, 9]):\n    print(index, value)',explain:'enumerate 同时给出索引和值。查找时找到就返回，遍历结束仍找不到则返回 -1。',steps:['完成 find_index(items, target)，返回第一次匹配的索引。','不存在时返回 -1。','不要用列表自带的 index，练习一次逐项查找。'],starter:'def find_index(items, target):\n    # 从左到右查找\n    return -1\n',solution:'def find_index(items, target):\n    for i, item in enumerate(items):\n        if item == target:\n            return i\n    return -1',test:'assert find_index([4,9,4],4)==0\nassert find_index([4,9,4],9)==1\nassert find_index([],1)==-1\nassert find_index([1],2)==-1',hint:'for i, item in enumerate(items):，找到时 return i。',reflection:'目标在最后和目标不存在，比较次数有什么相同点？'});
add({id:'ds-02',track:'structures',title:'哈希表：统计每个词出现几次',intro:'统计词频时，每看到一个词就更新它的计数。字典特别适合这种按键访问。',concept:'Python 字典通常基于哈希表，平均查找和更新可以视为 O(1)。总共处理 n 个词，整体通常为 O(n)，但仍需额外存储不同词。',example:'counts = {}\ncounts["代码"] = counts.get("代码", 0) + 1',explain:'get(word, 0) 使第一次遇到的词从 0 开始累加，避免 KeyError。',steps:['实现 frequencies(words)，返回词与次数的字典。','空输入返回 {}。','不区分其他格式，按原字符串准确计数。'],starter:'def frequencies(words):\n    counts = {}\n    # 在这里统计\n    return counts\n',solution:'def frequencies(words):\n    counts = {}\n    for word in words:\n        counts[word] = counts.get(word, 0) + 1\n    return counts',test:'assert frequencies(["a","b","a"])=={"a":2,"b":1}\nassert frequencies([])=={}\nassert frequencies(["A","a"])=={"A":1,"a":1}',hint:'循环每个 word，用 counts.get(word, 0) + 1 更新。',reflection:'如果需要忽略大小写，应该在哪一步转换？'});
add({id:'ds-03',track:'structures',title:'栈：实现括号配对检查',intro:'编辑器能发现漏写的括号。只检查一种圆括号时，可以用“后进先出”的栈。',concept:'栈像一摞盘子。append 放入顶部，pop 取出顶部。遇到左括号入栈，遇到右括号时需要有对应的左括号。',example:'stack = []\nstack.append("(")\nprint(stack.pop())',explain:'右括号出现时栈为空，说明没有匹配项；遍历结束栈还不空，说明左括号多了。其他字符本课忽略。',steps:['实现 balanced(text)，仅检查圆括号。','遇到多余的右括号立即返回 False。','最终栈为空才返回 True。'],starter:'def balanced(text):\n    stack = []\n    # 检查每个字符\n    return len(stack) == 0\n',solution:'def balanced(text):\n    stack = []\n    for char in text:\n        if char == "(":\n            stack.append(char)\n        elif char == ")":\n            if not stack:\n                return False\n            stack.pop()\n    return len(stack) == 0',test:'assert balanced("(a(b))") is True\nassert balanced(")(") is False\nassert balanced("(()") is False\nassert balanced("") is True',hint:'先判断 stack 是否为空，再 pop，避免空栈错误。',reflection:'只有左右括号数量相等，为什么还不够？'});
add({id:'ds-04',track:'structures',title:'队列：按先来后到处理任务',intro:'打印任务和消息通常按先来后到处理。队列就是“先进先出”。',concept:'collections.deque 适合从两端添加和取出。append 放到尾部，popleft 从头部取出。',example:'from collections import deque\nqueue = deque(["第一份", "第二份"])\nprint(queue.popleft())',explain:'对长列表反复 pop(0) 会移动后续元素，deque 的头部操作更适合队列。',steps:['实现 process(tasks)，将输入复制进 deque。','用 while 和 popleft 取出任务，加入 result。','返回处理顺序，不修改原列表。'],starter:'from collections import deque\ndef process(tasks):\n    queue = deque(tasks)\n    result = []\n    # 逐个处理\n    return result\n',solution:'from collections import deque\ndef process(tasks):\n    queue = deque(tasks)\n    result = []\n    while queue:\n        result.append(queue.popleft())\n    return result',test:'assert process(["a","b","c"])==["a","b","c"]\nassert process([])==[]\nx=[1,2];process(x);assert x==[1,2]',hint:'while queue: 下面写 result.append(queue.popleft())。',reflection:'如果改成 pop，会变成什么顺序？'});
add({id:'ds-05',track:'structures',title:'二分查找：每次排除一半',intro:'在已排序列表中寻找数字，可以每次比较中间元素，而不是从头看到尾。',concept:'二分查找要求数据有序。使用闭区间 [left, right]，中点小于目标就移动 left，否则移动 right。最坏比较次数为 O(log n)。',example:'left, right = 0, 9\nmid = (left + right) // 2\nprint(mid)',explain:'移动边界时要跳过已经比较的 mid，否则可能永远停在同一个位置。',steps:['实现 search(nums, target)，假设 nums 升序排列。','找到返回任意匹配索引，找不到返回 -1。','检查空列表、首尾元素。'],starter:'def search(nums, target):\n    left, right = 0, len(nums) - 1\n    # 在区间有效时继续\n    return -1\n',solution:'def search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1',test:'assert search([1,3,5,7],5)==2\nassert search([1,3,5,7],1)==0\nassert search([1,3,5,7],7)==3\nassert search([],1)==-1\nassert search([1,2],3)==-1',hint:'while left <= right；两边分别更新为 mid + 1 或 mid - 1。',reflection:'为什么无序列表不能直接用二分查找？'});
add({id:'ds-06',track:'structures',title:'图：追踪模块之间的依赖',intro:'项目里的模块会调用其他模块。这可以看成一个图：模块是节点，依赖是边。',concept:'用字典保存邻接表，如 {"api": ["service"]}。广度优先遍历需要队列和 visited 集合，后者避免循环依赖导致无限遍历。',example:'graph = {"api": ["service"], "service": ["db"]}\nprint(graph["api"])',explain:'在入队时标记已访问，可避免同一个节点多次进入队列。这个方法也能用于检查一个入口会影响哪些模块。',steps:['实现 reachable(graph, start)，返回包括起点在内可到达节点的集合。','使用 visited 防止循环。','不存在的邻接表按空列表处理。'],starter:'from collections import deque\ndef reachable(graph, start):\n    seen = {start}\n    queue = deque([start])\n    # 遍历依赖\n    return seen\n',solution:'from collections import deque\ndef reachable(graph, start):\n    seen = {start}\n    queue = deque([start])\n    while queue:\n        node = queue.popleft()\n        for neighbor in graph.get(node, []):\n            if neighbor not in seen:\n                seen.add(neighbor)\n                queue.append(neighbor)\n    return seen',test:'assert reachable({"a":["b"],"b":["a","c"]},"a")=={"a","b","c"}\nassert reachable({},"a")=={"a"}',hint:'弹出节点，遍历 graph.get(node, [])，未见过才标记并入队。',reflection:'节点“可达”是否意味着它一定会在本次请求中执行？'});
const SQL_SETUP="CREATE TABLE expenses(id INTEGER PRIMARY KEY, category TEXT NOT NULL, amount INTEGER NOT NULL); INSERT INTO expenses VALUES(1,'餐饮',20),(2,'交通',5),(3,'餐饮',35),(4,'学习',100); CREATE TABLE budgets(category TEXT PRIMARY KEY, budget INTEGER); INSERT INTO budgets VALUES('餐饮',80),('交通',30),('学习',150);";
const sql=l=>add({track:'sql',kind:'sql',setup:SQL_SETUP,...l});
sql({id:'sql-01',title:'SELECT：读取表里的数据',intro:'数据库中的表像带有明确列名的表格。练习数据库已经准备好 expenses 表：id、category、amount。金额单位是元。',concept:'SELECT 指定要读取的列，FROM 指定来源表。ORDER BY id 保证结果按 id 排列；没有排序时不能依赖默认顺序。',example:'SELECT id, category, amount\nFROM expenses\nORDER BY id;',explain:'表中有餐饮 20、交通 5、餐饮 35、学习 100 四笔记录。每次点击运行都会恢复这份初始数据。',steps:['只读取 category 和 amount 两列，顺序不要颠倒。','从 expenses 表查询。','按 id 升序排列。'],starter:'-- 选择两列，并按 id 排序\n',solution:'SELECT category, amount FROM expenses ORDER BY id;',expected:[['餐饮',20],['交通',5],['餐饮',35],['学习',100]],hint:'SELECT category, amount FROM expenses ORDER BY id;',reflection:'SELECT * 很方便，为什么接口中常常显式写出列名？'});
sql({id:'sql-02',title:'WHERE：筛选你关心的记录',intro:'想只看较大的支出，不需要把全部数据取出来再手动筛选。',concept:'WHERE 对每行应用条件。AND 表示两个条件都成立，OR 表示至少一个成立。文本值使用单引号。',example:"SELECT * FROM expenses WHERE category = '餐饮';",explain:'数据库金额为 20、5、35、100。>= 会包括等于的边界，> 不会。',steps:['读取 amount 大于等于 20 的记录。','返回 id、amount 两列。','按 amount 从大到小排列。'],starter:'SELECT id, amount\nFROM expenses\n-- 添加筛选与排序\n',solution:'SELECT id, amount FROM expenses WHERE amount >= 20 ORDER BY amount DESC;',expected:[[4,100],[3,35],[1,20]],hint:'WHERE amount >= 20 ORDER BY amount DESC。',reflection:'如果把 >= 改成 >，哪条记录会消失？'});
sql({id:'sql-03',title:'GROUP BY：按类别汇总',intro:'单笔支出之外，你可能更关心每类一共花了多少钱。',concept:'SUM(amount) 求和。GROUP BY category 按类别分组。WHERE 筛选原始行，HAVING 筛选聚合后的组。',example:'SELECT category, COUNT(*) AS count\nFROM expenses\nGROUP BY category;',explain:'聚合查询里，普通列通常需要出现在 GROUP BY 中。不要依赖 SQLite 容许的模糊列选择。',steps:['按 category 分组，计算 SUM(amount)。','第二列取名 total。','按 category 升序排序。'],starter:'-- 汇总每类金额\n',solution:'SELECT category, SUM(amount) AS total FROM expenses GROUP BY category ORDER BY category;',expected:[['交通',5],['学习',100],['餐饮',55]],hint:'SELECT category, SUM(amount) AS total ... GROUP BY category ORDER BY category。',reflection:'筛选总支出大于 50 的类别，该用 WHERE 还是 HAVING？'});
sql({id:'sql-04',title:'JOIN：连接两张相关的表',intro:'expenses 保存消费，budgets 保存预算。通过共同的 category 字段，可以在一行里比较二者。',concept:'JOIN 的 ON 指定关联条件。别名 e 和 b 缩短表名。漏写正确关联条件可能导致行数成倍增加。',example:'SELECT e.id, b.budget\nFROM expenses e\nJOIN budgets b ON e.category = b.category;',explain:'本课每个预算类别只有一条记录。连接后按类别分组，就能同时看到总花费和预算。',steps:['连接 expenses 和 budgets，关联 category。','返回 category、总 amount、budget。','按 category、budget 分组，再按 category 排序。'],starter:'-- 将支出和预算连接后汇总\n',solution:'SELECT e.category, SUM(e.amount), b.budget FROM expenses e JOIN budgets b ON e.category = b.category GROUP BY e.category, b.budget ORDER BY e.category;',expected:[['交通',5,30],['学习',100,150],['餐饮',55,80]],hint:'关联条件为 e.category = b.category。聚合列用 SUM(e.amount)。',reflection:'某个类别没有预算时，INNER JOIN 会保留它吗？LEFT JOIN 呢？'});
sql({id:'sql-05',title:'INSERT 与 UPDATE：修改练习数据',intro:'数据库不仅可以读，还可以新增与修改记录。先用练习数据理解这些动作。',concept:'INSERT INTO 表(列) VALUES(值) 新增一行。UPDATE 表 SET 列=值 WHERE 条件 修改匹配的行。忘记 WHERE 会更新所有行。',example:"INSERT INTO expenses(id, category, amount) VALUES(5, '交通', 10);\nUPDATE expenses SET amount = 8 WHERE id = 2;",explain:'本网站每次运行前都重建数据库，方便重复试验；真实业务系统的修改不会自动重置。',steps:['新增 id=5、category="学习"、amount=50 的记录。','把 id=2 的 amount 改为 8。','查询 id 和 amount，按 id 排序验证。'],starter:'-- 先新增，再定向更新，最后查询\n',solution:"INSERT INTO expenses(id, category, amount) VALUES(5, '学习', 50);\nUPDATE expenses SET amount = 8 WHERE id = 2;\nSELECT id, amount FROM expenses ORDER BY id;",verify:'SELECT id, amount FROM expenses ORDER BY id',expected:[[1,20],[2,8],[3,35],[4,100],[5,50]],hint:'两条修改语句都以分号结束；UPDATE 一定加 WHERE id = 2。',reflection:'如何在真实数据库中先确认 WHERE 会匹配哪些行？'});
sql({id:'sql-06',title:'事务：一组动作一起成功',intro:'转账既要扣款也要入账。如果只执行一半，就会出现错误。事务把多个动作组织成一组。',concept:'BEGIN 开始事务，COMMIT 提交，ROLLBACK 撤销本次事务内尚未提交的修改。事务不是自动备份，也不能撤销所有历史操作。',example:'BEGIN;\nUPDATE expenses SET amount = 999 WHERE id = 1;\nROLLBACK;',explain:'执行 ROLLBACK 后，id=1 的金额应该还是 20。它不是把数据库清空，而是撤销当前事务。',steps:['开始事务。','把 id=1 的 amount 改为 999。','回滚，再查询 id=1 的 amount，应得到 20。'],starter:'-- 在事务中修改，然后回滚\n',solution:'BEGIN;\nUPDATE expenses SET amount = 999 WHERE id = 1;\nROLLBACK;\nSELECT amount FROM expenses WHERE id = 1;',verify:'SELECT amount FROM expenses WHERE id = 1',expected:[[20]],hint:'顺序为 BEGIN → UPDATE → ROLLBACK → SELECT。',reflection:'如果先 COMMIT，再 ROLLBACK，还能撤销已经提交的修改吗？'});
sql({id:'sql-07',title:'建表、约束与索引',intro:'数据库结构需要表达业务规则，比如任务标题不能为空、编号不能重复。',concept:'PRIMARY KEY 标识唯一记录。NOT NULL 禁止空值。CHECK 约束取值范围。索引加快某些查询，也增加存储和写入成本。',example:'CREATE TABLE tasks (\n  id INTEGER PRIMARY KEY,\n  title TEXT NOT NULL,\n  done INTEGER NOT NULL CHECK(done IN (0,1))\n);',explain:'空字符串与 NULL 不一样。本课为 done 添加合法取值约束，再为常用筛选字段创建索引。',steps:['按示例创建 tasks 表。','新增 id=1、title="读源码"、done=0。','创建 idx_tasks_done 索引，索引字段为 done，并查询记录。'],starter:'-- 创建表、添加记录、创建索引\n',solution:"CREATE TABLE tasks(id INTEGER PRIMARY KEY, title TEXT NOT NULL, done INTEGER NOT NULL CHECK(done IN (0,1)));\nINSERT INTO tasks VALUES(1, '读源码', 0);\nCREATE INDEX idx_tasks_done ON tasks(done);\nSELECT title, done FROM tasks ORDER BY id;",verify:"SELECT title, done FROM tasks WHERE EXISTS(SELECT 1 FROM sqlite_master WHERE type='index' AND name='idx_tasks_done') ORDER BY id",expected:[['读源码',0]],hint:'CREATE INDEX idx_tasks_done ON tasks(done); 用于创建索引。',reflection:'为什么不能给每个字段都盲目添加索引？怎样用查询计划验证收益？'});
