guide({id:'sai-01',track:'springai',title:'理解模型调用与 Spring AI',intro:'Spring AI 帮 Java 应用连接模型、工具和检索组件。它不是模型本身，也不保证回答正确。',concept:'应用发送消息，模型生成输出。模型服务可能来自云端，也可能部署在本机。依赖版本、模型名称、接口地址必须匹配所用提供方。',example:'业务输入 → ChatClient → 模型提供方\n业务输出 ← 解析与校验 ← 模型响应',explain:'这是调用关系示意。当前官方参考为 Spring AI 2.x 系列；通过 start.spring.io 选择兼容的稳定组合，不手动混用旧版 artifact 名称。本机模型无需云端密钥，但需要下载模型和足够资源。',steps:['打开 Spring AI 官方 Getting Started 与 Initializr。','选择一个模型提供方，记下它需要的配置与成本限制。','生成项目，核对 pom.xml 中的 Spring AI 依赖和 Spring Boot 版本。'],checks:['知道模型与框架的区别','选择了明确的模型提供方','能定位模型相关依赖'],quiz:{q:'接入 Spring AI 后，模型输出能直接当可信业务指令吗？',options:['可以，框架保证内容正确','不可以，仍需校验、权限与评估','所有输出都是数据库记录'],answer:1,explanation:'框架解决集成问题，不能把概率生成的内容变成可靠事实。'},hint:'先完成单次文本调用，再考虑工具与多 Agent。',reflection:'调用失败来自网络、配额、配置还是输出解析？你如何区分？',source:'https://docs.spring.io/spring-ai/reference/getting-started.html'});
guide({id:'sai-02',track:'springai',title:'ChatClient：完成一次真实对话',intro:'先实现一个简单的文本助手，并追踪请求从 Controller 到模型的路径。',concept:'ChatClient.Builder 创建客户端。prompt().user(...).call().content() 完成一次同步调用。真实调用需要配置可用模型。',example:'package com.example.demo;\nimport org.springframework.ai.chat.client.ChatClient;\nimport org.springframework.stereotype.Service;\n@Service\npublic class TutorService {\n    private final ChatClient chat;\n    public TutorService(ChatClient.Builder builder) {\n        this.chat = builder.defaultSystem("用中文简短解释编程概念；不确定时说明。").build();\n    }\n    public String explain(String topic) {\n        return chat.prompt().user(topic).call().content();\n    }\n}',explain:'此文件需要放在已经配置模型 starter 的项目中。配置在服务端读取；不要把 API Key 放到浏览器代码。等待模型会占用时间，需要记录耗时并为生产接口设置合理超时。',steps:['按项目指南配置一个云端或本机模型。','添加 TutorService，由 Controller 调用 explain。','提交“解释变量”，记录响应、耗时与失败时的错误。'],checks:['至少一次真实模型调用成功','能定位提供方配置','能解释一次调用的完整流程'],quiz:{q:'content() 返回内容为空时，应该怎样处理？',options:['无条件当成功','按业务契约检查并返回可理解的错误','直接无限重试'],answer:1,explanation:'空内容、超时和解析失败都应成为可观察、有限处理的失败路径。'},hint:'先检查模型服务可用，再核对项目日志，避免盲目修改业务代码。',reflection:'这个同步调用会在哪一步等待？并发增大时会产生什么影响？',source:'https://docs.spring.io/spring-ai/reference/api/chatclient.html'});
guide({id:'sai-03',track:'springai',title:'结构化输出：把回答转成可验证的数据',intro:'如果后续代码需要任务标题与优先级，自由文本很难可靠处理。结构化输出使字段更明确。',concept:'Java record 描述输出结构，entity(...) 将模型输出转换成类型。转换成功只代表结构可解析，不代表业务内容真实。',example:'record Plan(String title, int priority) {}\n\nPlan plan = chat.prompt()\n    .user("把学习 Python 整理成一个任务，priority 为 1 到 3")\n    .call()\n    .entity(Plan.class);',explain:'这段代码在已经注入 ChatClient 的服务方法内使用。转换后仍要校验 title 非空、priority 在 1..3 之间，再交给下游。',steps:['为项目添加 Plan record。','把输出转换为 Plan，再加入业务字段校验。','准备异常样例：空标题、越界优先级、模型超时。'],checks:['输出可以解析为 Plan','范围校验独立于模型提示','失败不会直接写入数据库'],quiz:{q:'提示词写“priority 必须为 1 到 3”，是否代替代码校验？',options:['不能，提示词不是强制约束','可以，无需测试','只要用英文就可以'],answer:0,explanation:'生成过程与业务可信边界不同，代码必须执行明确检查。'},hint:'先判断 plan 是否为空，再检查它的每个业务字段。',reflection:'结构正确、业务有效、事实正确分别是什么层次？',source:'https://docs.spring.io/spring-ai/reference/api/structured-output-converter.html'});
guide({id:'sai-04',track:'springai',title:'工具调用：模型提议，程序执行',intro:'模型可以选择一个工具，但真正执行操作的是你的程序。权限和参数校验必须放在执行边界。',concept:'@Tool 暴露一项能力。工具描述帮助模型选择工具，程序仍需控制哪些操作允许执行。纯读取工具适合作为第一次练习。',example:'import org.springframework.ai.tool.annotation.Tool;\n\nclass StudyTools {\n    @Tool(description = "读取当前学习计划的标题")\n    public String currentPlan() { return "Python 基础"; }\n}\n\n// 在已经有 chat 的服务中调用\n// chat.prompt().user("我目前学什么？")\n//     .tools(new StudyTools()).call().content();',explain:'示例工具返回固定学习计划，用来验证调用机制。不要把任意 shell 命令执行能力直接交给模型，也不要只依赖提示词保护写操作。',steps:['把 StudyTools 放入项目并注册到一次调用。','询问当前学习计划，观察模型是否实际触发工具。','在工具方法内记录一次不含敏感信息的调用日志。'],checks:['能看到工具真实调用的证据','能区分模型文字与工具结果','能说清写操作的授权边界'],quiz:{q:'工具参数来自模型时，应当视作什么？',options:['始终可信的数据','需要校验的外部输入','已经经过业务审批的指令'],answer:1,explanation:'模型可能误解任务或受到不可信内容影响，执行边界必须有独立约束。'},hint:'模型没调用工具时，先查看工具描述与请求需求是否明确匹配。',reflection:'如果工具会扣款或删除记录，你应在哪一层阻止未批准调用？',source:'https://docs.spring.io/spring-ai/reference/api/tools.html'});
guide({id:'sai-05',track:'springai',title:'RAG、评估与可观察性',intro:'让模型回答项目文档，需要先找到相关证据，再生成答案。把所有文档塞进提示词通常不可行。',concept:'RAG 是“检索后再生成”。切块、向量化、召回、重排和引用各有独立作用。评估要分别检查有没有找到证据和有没有忠实回答。',example:'问题 → 检索文档片段 → 拼接带来源的上下文\n     → 模型回答 → 引用核对 → 评估记录\n\n评估字段：问题、预期证据、实际证据、答案、耗时、费用',explain:'先用 5 份小文档做最小验证，再增加数据量。记录 token 用量与提供方价格，不能仅凭调用次数估算费用。没有证据时应明确说不知道。',steps:['准备 5 份不含隐私的小文档。','编写 10 个问题，其中至少 3 个不在文档中。','用官方检索组件完成原型，逐项记录召回、回答、引用和失败原因。'],checks:['有固定评估问题集','未知问题能被识别','回答引用能回溯原文','记录过耗时和使用量'],quiz:{q:'回答错了，为什么先检查检索结果？',options:['检索缺失与生成错误需要不同修复','模型永远不会错','只要扩大上下文就一定正确'],answer:0,explanation:'没有召回正确证据和忽略正确证据，是两种不同失败。'},hint:'先查看实际传给模型的文档片段，再分析模型输出。',reflection:'你的评估能发现“答案流畅但引用无关”吗？',source:'https://docs.spring.io/spring-ai/reference/api/retrieval-augmented-generation.html'});
add({id:'ag-01',track:'agent',title:'拆开 Agent：状态、决策、动作',intro:'先不用真实模型，写一个确定性的决策器，理解 Agent 为什么不只是一次聊天调用。',concept:'Agent 循环通常观察状态、选择动作、执行工具、更新状态并检查停止条件。本课是流程模拟，不具备真实语言理解能力。',example:'state = {"has_data": False}\naction = "search" if not state["has_data"] else "answer"\nprint(action)',explain:'先把流程写清楚，再替换决策来源。否则模型调用一多，你会不知道错误发生在决策、工具还是状态更新。',steps:['实现 decide(state)。','has_data 为 False 时返回 "search"；为 True 时返回 "answer"。','缺少 has_data 时按 False 处理。'],starter:'def decide(state):\n    # 选择下一步动作\n    pass\n',solution:'def decide(state):\n    if state.get("has_data", False):\n        return "answer"\n    return "search"',test:'assert decide({})=="search"\nassert decide({"has_data":False})=="search"\nassert decide({"has_data":True})=="answer"',hint:'用 state.get("has_data", False) 读取状态。',reflection:'把 decide 换成模型调用后，哪些行为必须继续由代码保证？'});
add({id:'ag-02',track:'agent',title:'工具注册与参数校验',intro:'工具不是把模型文本直接当代码运行，而是从事先允许的操作中选择一个。',concept:'工具注册表把名称映射到可调用函数。执行前检查名称和参数；未知工具应该拒绝，而不是猜测一个替代动作。',example:'tools = {"double": lambda x: x * 2}\nprint(tools["double"](3))',explain:'lambda 是简短函数。本课只允许 add 工具，参数必须是两个整数；bool 在 Python 中属于 int 的子类，因此严格检查时可使用 type(x) is int。',steps:['实现 call_tool(name, args)。','只允许 name="add" 且 args 中 a、b 都为 int，返回其和。','其他情况抛出 ValueError。'],starter:'def call_tool(name, args):\n    # 先验证，再执行\n    pass\n',solution:'def call_tool(name, args):\n    if name != "add" or not isinstance(args, dict):\n        raise ValueError("未知工具或参数格式错误")\n    a, b = args.get("a"), args.get("b")\n    if type(a) is not int or type(b) is not int:\n        raise ValueError("参数必须是整数")\n    return a + b',test:'assert call_tool("add",{"a":2,"b":3})==5\nfor name,args in [("delete",{}),("add",{"a":"2","b":3}),("add",{"a":True,"b":2})]:\n    try: call_tool(name,args)\n    except ValueError: pass\n    else: raise AssertionError("非法工具或参数必须被拒绝")',hint:'先检查工具名和字典，再检查 a、b 的类型。',reflection:'为什么不能使用 eval(model_output) 执行模型输出？'});
add({id:'ag-03',track:'agent',title:'停止条件：防止无限调用',intro:'Agent 可能反复选择同一动作。即使模型表现正常，也要设置可验证的步数上限。',concept:'预算可以限制步数、耗时、token 或费用。停止由代码执行，不应该只在提示词里要求“不要循环太久”。',example:'for step in range(3):\n    print("执行第", step + 1, "步")',explain:'本课使用预先给定的动作列表模拟模型输出，遇到 done 立即停止；否则最多处理 limit 个动作。',steps:['实现 run(actions, limit)。','收集动作；遇到 "done" 时停止且不把 done 加入结果。','最多读取 limit 项；limit=0 时返回 []。'],starter:'def run(actions, limit):\n    result = []\n    # 有限执行\n    return result\n',solution:'def run(actions, limit):\n    result = []\n    for action in actions[:max(0, limit)]:\n        if action == "done":\n            break\n        result.append(action)\n    return result',test:'assert run(["search","done","write"],5)==["search"]\nassert run(["a","b","c"],2)==["a","b"]\nassert run(["a"],0)==[]',hint:'先限制遍历范围，再在循环中判断 done。',reflection:'达到上限时，界面应该怎样向用户解释未完成部分？'});
add({id:'ag-04',track:'agent',title:'检索：先找到证据再回答',intro:'最简单的检索不需要向量数据库。先用明确的关键词匹配，理解证据选择的流程。',concept:'检索器返回文档片段及其来源，而不是凭空生成事实。向量检索能改善语义匹配，但也需要召回与相关性评估。',example:'docs = [{"id": "d1", "text": "Python 是编程语言"}]\nprint("Python" in docs[0]["text"])',explain:'本课只是区分大小写的字面匹配示例，不是完整语义 RAG。没有匹配返回空列表，后续生成应据此拒绝编造。',steps:['实现 retrieve(docs, query)。','返回 text 包含 query 的文档 id，保持原顺序。','空 query 返回 []，避免误匹配所有文档。'],starter:'def retrieve(docs, query):\n    # 返回相关文档编号\n    return []\n',solution:'def retrieve(docs, query):\n    if not query:\n        return []\n    return [doc["id"] for doc in docs if query in doc["text"]]',test:'d=[{"id":"a","text":"Python 入门"},{"id":"b","text":"Java 入门"}]\nassert retrieve(d,"Python")==["a"]\nassert retrieve(d,"SQL")==[]\nassert retrieve(d,"")==[]',hint:'先处理空查询，再遍历每份文档的 text。',reflection:'字面不相同但意思接近时，本课检索会失败吗？如何测出来？'});
add({id:'ag-05',track:'agent',title:'证据引用与未知回答',intro:'回答“有来源”不只是附上一个链接，而是让每个关键说法能够追溯到相关证据。',concept:'引用标识应来自实际检索结果。无证据时明确返回未知，比生成流畅但无依据的答案更可靠。',example:'evidence = [{"id": "doc1", "text": "课程共三节"}]\nprint(evidence[0]["text"] + " [doc1]")',explain:'本课用原文摘取模拟生成，专注证据格式。真实模型还需要检查回答是否被证据支持。',steps:['实现 answer(evidence)。','没有证据返回 "没有足够证据"。','有证据返回第一条 text，后接空格和 [id]。'],starter:'def answer(evidence):\n    pass\n',solution:'def answer(evidence):\n    if not evidence:\n        return "没有足够证据"\n    item = evidence[0]\n    return f"{item[\'text\']} [{item[\'id\']}]"',test:'assert answer([])=="没有足够证据"\nassert answer([{"id":"d2","text":"原文"}])=="原文 [d2]"',hint:'先处理空列表，再读取 evidence[0]。',reflection:'来源确实存在，为什么仍不能保证回答忠实于来源？'});
add({id:'ag-06',track:'agent',title:'固定评估集：用案例验证 Agent',intro:'一次成功演示不能代表稳定可靠。固定评估集让改动前后的效果可以比较。',concept:'本课用准确匹配评估简单结果；复杂回答还需人工检查忠实性与完整性。指标计算规则必须明确，包括空测试集。',example:'expected = ["a", "b", "c"]\nactual = ["a", "x", "c"]\nprint(2 / 3)',explain:'准确率只衡量本组案例，不代表所有真实场景。错误拒答、错误执行工具、费用和耗时也需要单独记录。',steps:['实现 accuracy(expected, actual)，返回匹配数量除以总数量。','空 expected 返回 0.0。','长度不同抛出 ValueError，避免 zip 悄悄截断造成虚高。'],starter:'def accuracy(expected, actual):\n    pass\n',solution:'def accuracy(expected, actual):\n    if len(expected) != len(actual):\n        raise ValueError("长度不一致")\n    if not expected:\n        return 0.0\n    correct = sum(a == b for a, b in zip(expected, actual))\n    return correct / len(expected)',test:'assert accuracy([1,2],[1,3])==0.5\nassert accuracy([],[])==0.0\ntry: accuracy([1],[])\nexcept ValueError: pass\nelse: raise AssertionError("长度不同必须报错")',hint:'先检查长度，再处理空集，最后累计比较结果。',reflection:'为什么评估题不能全部来自开发时已看过的例子？'});
add({id:'ma-01',track:'multi',title:'多角色分工与输入输出契约',intro:'把一个任务拆成研究、撰写和复核，需要明确每个角色交付什么，而不是只给它们不同称呼。',concept:'多角色工作流不一定需要多个模型。先用普通函数验证信息流，再判断独立 Agent 是否带来可测量收益。',example:'research = {"facts": ["证据一"], "sources": ["doc1"]}\n# 写作者消费 facts 与 sources，而不是猜测研究结果',explain:'本课检查研究结果是否符合结构要求，模拟交接边界。字段类型不正确时必须阻止进入下一步。',steps:['实现 valid_report(report)。','必须是字典，facts 和 sources 都是非空字符串列表。','两列表长度应相同，其他情况返回 False。'],starter:'def valid_report(report):\n    pass\n',solution:'def valid_report(report):\n    if not isinstance(report, dict):\n        return False\n    facts, sources = report.get("facts"), report.get("sources")\n    if not isinstance(facts, list) or not isinstance(sources, list):\n        return False\n    return bool(facts) and len(facts) == len(sources) and all(isinstance(x,str) and x.strip() for x in facts + sources)',test:'assert valid_report({"facts":["a"],"sources":["d1"]})\nassert not valid_report({"facts":[],"sources":[]})\nassert not valid_report({"facts":["a"],"sources":[]})\nassert not valid_report({"facts":"a","sources":["d1"]})',hint:'先检查容器类型，再检查每项内容和长度对应关系。',reflection:'只给角色取名“专家”，能替代明确交付契约吗？'});
add({id:'ma-02',track:'multi',title:'状态机：让协作顺序可检查',intro:'研究还没完成就开始审查，流程会变得混乱。状态机明确允许的下一步。',concept:'状态机用有限状态和转移规则描述流程。非法转移应被拒绝，不让任意角色随意修改全局状态。',example:'transitions = {"research": "write", "write": "review", "review": "done"}\nprint(transitions["research"])',explain:'真实工作流还可能包含需要修改、失败和人工批准等状态。本课先实现最小的严格顺序。',steps:['实现 transition(current, requested)。','只允许 research→write→review→done。','非法转移抛 ValueError；done 没有下一步。'],starter:'def transition(current, requested):\n    allowed = {"research": "write", "write": "review", "review": "done"}\n    # 检查请求是否被允许\n',solution:'def transition(current, requested):\n    allowed = {"research": "write", "write": "review", "review": "done"}\n    if current not in allowed or allowed[current] != requested:\n        raise ValueError("非法状态转移")\n    return requested',test:'assert transition("research","write")=="write"\nfor a,b in [("research","done"),("done","write"),("x","y")]:\n    try: transition(a,b)\n    except ValueError: pass\n    else: raise AssertionError("应拒绝非法转移")',hint:'allowed.get(current) 必须与 requested 一致，同时确认 current 是已知状态。',reflection:'复核失败应该回到哪一步？如何限制往返次数？'});
add({id:'ma-03',track:'multi',title:'汇总结果：去重并保留来源',intro:'多个研究角色可能找到同一段证据。直接拼接会让上下文重复、费用增加。',concept:'汇总需要确定性规则。本课按 id 去重，第一次出现的内容优先，并保留原始顺序。',example:'seen = set()\nif "doc1" not in seen:\n    seen.add("doc1")',explain:'真实系统遇到同 id 不同内容时，应记录冲突；本课采用明确的“首次优先”简化规则。',steps:['实现 merge(groups)，groups 是多个文档列表。','按文档 id 去重。','返回保留完整文档对象的新列表，不修改输入。'],starter:'def merge(groups):\n    result, seen = [], set()\n    # 汇总\n    return result\n',solution:'def merge(groups):\n    result, seen = [], set()\n    for group in groups:\n        for doc in group:\n            if doc["id"] not in seen:\n                seen.add(doc["id"])\n                result.append(doc)\n    return result',test:'a={"id":"a","text":"1"};b={"id":"b","text":"2"}\nassert merge([[a],[a,b]])==[a,b]\nassert merge([])==[]\nassert merge([[a],[{"id":"a","text":"other"}]])==[a]',hint:'两层循环：先遍历组，再遍历每组的文档。',reflection:'如何向用户展示互相矛盾的来源，而不是悄悄选择一个？'});
add({id:'ma-04',track:'multi',title:'失败与重试：只重试该重试的错误',intro:'网络偶发失败可以重试，但非法参数重试一百次也不会变好。',concept:'重试必须有限，且只对可恢复错误执行。具有写入副作用的操作还需要幂等性，防止重复创建或重复扣费。',example:'try:\n    raise TimeoutError("暂时超时")\nexcept TimeoutError:\n    print("可以按预算重试")',explain:'本课不等待真实网络，仅用可调用函数模拟超时。attempts 表示总尝试次数，不是第一次之外的次数。',steps:['实现 retry(fn, attempts)，成功时立刻返回结果。','只捕获 TimeoutError；用尽次数后重新抛出最后的异常。','attempts 小于 1 时抛 ValueError。'],starter:'def retry(fn, attempts):\n    pass\n',solution:'def retry(fn, attempts):\n    if attempts < 1:\n        raise ValueError("至少尝试一次")\n    for i in range(attempts):\n        try:\n            return fn()\n        except TimeoutError:\n            if i == attempts - 1:\n                raise',test:'assert retry(lambda: 7,2)==7\nn=[0]\ndef flaky():\n    n[0]+=1\n    if n[0]<2: raise TimeoutError()\n    return 9\nassert retry(flaky,3)==9 and n[0]==2\ntry: retry(lambda: 1,0)\nexcept ValueError: pass\nelse: raise AssertionError("次数必须有效")',hint:'for 中 try 调用，except 内判断是否已是最后一次。',reflection:'为什么自动重试创建订单，可能产生重复订单？'});
add({id:'ma-05',track:'multi',title:'预算与日志：协作需要付出什么',intro:'多个角色会增加调用数量、等待时间和中间结果。更多 Agent 不一定更好。',concept:'全局预算应覆盖所有角色，而不是让每个角色各用一份不受约束的额度。日志应记录角色、输入标识、输出标识、耗时和费用。',example:'events = [{"role": "research", "tokens": 120}, {"role": "review", "tokens": 80}]\nprint(sum(e["tokens"] for e in events))',explain:'token 数量不是货币金额；实际费用还与模型、输入输出价格和缓存等有关。本课只检查一个整数预算。',steps:['实现 within_budget(events, limit)。','如果有负 token 值，抛 ValueError。','总 token 不超过 limit 返回 True，否则 False。'],starter:'def within_budget(events, limit):\n    pass\n',solution:'def within_budget(events, limit):\n    total = 0\n    for event in events:\n        tokens = event["tokens"]\n        if tokens < 0:\n            raise ValueError("用量不能为负")\n        total += tokens\n    return total <= limit',test:'assert within_budget([{"tokens":3},{"tokens":2}],5) is True\nassert within_budget([{"tokens":6}],5) is False\nassert within_budget([],0) is True\ntry: within_budget([{"tokens":-1}],5)\nexcept ValueError: pass\nelse: raise AssertionError("应拒绝负用量")',hint:'累计所有事件，再比较 <=；边界恰好等于预算应允许。',reflection:'并行调用都在开始前看见剩余预算，如何避免一起超支？'});
